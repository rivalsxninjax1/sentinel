# Local Vulnerable Lab

Not yet built (introduced alongside relevant detection phases, Docker-based). Will house
intentionally vulnerable targets for XSS, SQLi, SSRF, SSTI, XXE, IDOR, CSRF, CORS, JWT,
file upload, path traversal, command injection, GraphQL, WebSocket, open redirect, and
prototype pollution scenarios, each paired with a regression test.
