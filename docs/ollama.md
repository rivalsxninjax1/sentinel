# Local Ollama Reasoning Layer

Not yet implemented (Phase 4). Will document: `LLMProvider`/`OllamaProvider`
abstraction, configured model(s), structured-output schemas (Pydantic), validation /
repair / fallback behavior, and the two roles Ollama plays (recon classification, test
prioritization) per `docs/architecture.md` §7.
